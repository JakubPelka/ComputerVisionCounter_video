# models/

Place local YOLO-compatible model weights here.

Examples:

```text
yolo11x.pt
custom_model.pt
```

Model files are ignored by Git and should not be committed.

Reason:

- model files can be large,
- model licenses may differ from the application license,
- users should choose and manage their own weights,
- the repository should stay lightweight.
