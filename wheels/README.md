# wheels/

Optional local folder for offline Python wheel files.

The current launcher can use this folder when offline installation is needed.

Wheel files are ignored by Git because they are large generated/downloaded dependency artifacts.
